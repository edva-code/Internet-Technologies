# Internet-Technologies

Repository for all the lab works and tasks for my Internet Technologies course.

## Where things stand

Working on **Lab 1: XML, JSON and XPath** (deadline: October 9). The task
description is in `Laboratorinis 1.pdf`.

Chosen topic: a vehicle dealership (cars and motorcycles). This same topic
has to be reused for all four labs.

- `vehicles.xml` — the XML document for the dealership. Has cars and
  motorcycles, each with an engine, a list of features, and a mix of
  string/int/float/date data. Uses both a default namespace and two
  explicit ones (`car:`, `moto:`).
- `vehicles.json` — the same data as JSON, for comparison.

Still to do: the XPath part of the lab (writing and explaining queries
against `vehicles.xml`).
